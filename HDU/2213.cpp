#include<cstdio>
#include<iostream>
#include<algorithm>
#define LL long long int
int n, m, T;
char maz[105][105];
struct
int main(){
    while( scanf("%d %d %d%*c", &n, &m, &T) != EOF ){
        for(int i = 0; i < n; ++i) {
            for(int j = 0; j < m; ++j){
                maz[i][j] = getchar();
                if(maz[i][j]
            }
            getchar();
        }
        
    }
}
